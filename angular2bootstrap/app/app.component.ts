import {Component} from 'angular2/core';
import {MenuComponent} from 'app/menu.component';
import {ItemComponent} from 'app/item.component';
//import {Http, HTTP_BINDINGS, HTTP_PROVIDERS} from 'angular2/http'

@Component({
    selector: 'ce-app',
    template: ''
})
export class AppComponent {

	// LOAD SERVICE
	/*
	groups: Array<any>;
	components: Array<any>;

	constructor(http: Http) {
		http.get('app/data/groups.json').subscribe(res => {
			this.groups = res.json()
		});
		http.get('app/data/components.json').subscribe(res => {
			this.components = res.json()
		});
    }
    */

}